Yes! 🔥 **Milestone 3 is where we finally touch the signup experience**, but keep it tightly scoped: **role selection + role-specific registration data + connection to the database foundation from Milestone 2**.

Do **not** let the agent implement full RBAC yet. That comes in Milestone 4.

Give your teammate this exact prompt:

You are continuing the existing SecondLook project.

# MILESTONE 03 — ROLE-BASED SIGNUP

Milestone 01 — RBAC Architecture Audit has been completed and approved.

Milestone 02 — Database User / Role Foundation has been completed and approved.

Before changing anything, read:

* `docs/RBAC_ARCHITECTURE.md`
* `docs/MILESTONE_STATUS.md`
* `docs/HANDOFF.md`

Also inspect the actual implementation from Milestone 02.

Your job in this milestone is ONLY to implement the signup experience where a new user chooses whether they are:

```
BIDDER
OFFICER
```

and the corresponding role/profile data is stored using the database architecture established in Milestone 02.

DO NOT implement full RBAC authorization yet.

DO NOT implement separate dashboards yet.

DO NOT redesign the landing page yet.

DO NOT start Milestone 04.

==================================================

## 1. INSPECT CURRENT AUTHENTICATION FIRST

==================================================

Before modifying signup, inspect the existing authentication implementation.

Determine:

* current signup page
* current login page
* authentication provider
* Supabase Auth usage
* frontend auth state
* existing API client
* backend signup endpoints, if any
* existing user creation flow
* existing error handling
* existing form validation
* existing navigation after signup

Reuse the existing authentication system.

IMPORTANT:

If Supabase Auth is already being used, continue using it.

DO NOT create:

* custom password storage
* a second authentication system
* a second JWT system
* a second session system

Authentication identity and application role/profile data must remain separate concepts.

==================================================

## 2. SIGNUP UX

==================================================

Modify the existing signup experience instead of creating a completely separate authentication system.

The user must explicitly choose:

```
Account Type

[ Bidder ]
[ Officer ]
```

The selection must be required.

Do NOT silently assign a default role.

Do NOT default every new account to BIDDER.

The user should clearly understand what each account type is for.

Suggested UI:

```
Create your SecondLook account

I am signing up as:

┌──────────────────────┐
│       BIDDER         │
│ Submit bids and      │
│ track compliance     │
└──────────────────────┘

┌──────────────────────┐
│       OFFICER        │
│ Manage tenders and   │
│ review bidders       │
└──────────────────────┘
```

Then show the appropriate registration fields.

Use the existing application's visual language.

Do not redesign the entire application.

==================================================

## 3. BIDDER SIGNUP

==================================================

When BIDDER is selected, collect the minimum information required by the Milestone 02 bidder profile.

Use the actual fields established in the database model.

Do NOT invent unnecessary fields.

At minimum, if supported by the existing schema, this may include:

* company name
* GST number
* PAN number
* contact information

But the actual implementation must follow the existing Milestone 02 schema.

The flow should be:

```
Signup form
    ↓
Create Auth identity
    ↓
Create application user
    ↓
Assign BIDDER role
    ↓
Create/link bidder profile
    ↓
Return successful signup
```

Do not create a duplicate bidder domain entity if Milestone 02 determined that an existing bidder entity should be reused.

==================================================

## 4. OFFICER SIGNUP

==================================================

When OFFICER is selected, collect only the officer profile fields established in Milestone 02.

Potential fields may include:

* name
* department
* designation
* contact information

Use the actual Milestone 02 schema.

Flow:

```
Signup form
    ↓
Create Auth identity
    ↓
Create application user
    ↓
Assign OFFICER role
    ↓
Create/link officer profile
    ↓
Return successful signup
```

Do not introduce additional roles.

==================================================

## 5. ROLE MUST BE BACKEND CONTROLLED

==================================================

This is extremely important.

The frontend must NOT be trusted to create arbitrary roles.

Do NOT allow a request such as:

```
role = "ADMIN"
```

to create an administrator.

The backend must validate that the requested role is one of:

```
BIDDER
OFFICER
```

and reject anything else.

The backend should also validate that:

```
BIDDER → bidder profile
```

and:

```
OFFICER → officer profile
```

according to the Milestone 02 architecture.

Do not implement general role authorization middleware yet.

That belongs to Milestone 04.

==================================================

## 6. PREVENT INCONSISTENT ACCOUNT CREATION

==================================================

Handle failures safely.

For example:

```
Auth account created
      ↓
application user creation fails
```

Do not leave the system in an uncontrolled partial state if the existing authentication architecture supports rollback/cleanup.

Likewise:

```
BIDDER selected
      ↓
bidder profile creation fails
```

The error must be surfaced clearly.

Use the project's existing transaction/error-handling conventions.

Do not invent a complicated distributed transaction system.

Document any unavoidable limitation.

==================================================

## 7. VALIDATION

==================================================

Implement appropriate frontend and backend validation.

Examples:

* required email
* required password according to existing auth rules
* required role
* required role-specific fields
* valid field formats where existing project conventions support them
* duplicate account handling
* duplicate profile handling

Do not put security-critical validation only in React.

Backend validation is authoritative.

==================================================

## 8. ERROR HANDLING

==================================================

The signup UI should provide understandable errors.

Examples:

```
"Please select an account type."

"An account with this email already exists."

"Company name is required for bidder accounts."

"Department is required for officer accounts."
```

Do not expose:

* database stack traces
* SQL errors
* internal exceptions
* secrets
* authentication tokens

Use the existing project's error-response conventions.

==================================================

## 9. SUCCESS FLOW

==================================================

After successful signup, use the existing authentication/navigation architecture.

Do NOT build the bidder or officer dashboards yet.

For this milestone, the success destination can be a temporary/existing authenticated page if necessary.

The important thing is that the account has been created correctly with:

```
identity
role
profile
```

Do not create dashboard-specific logic just to make the signup flow look complete.

Document what the current post-signup destination is.

==================================================

## 10. LOGIN COMPATIBILITY

==================================================

Do not redesign login.

However, verify that accounts created through the new signup flow can still use the existing login system.

Test both:

```
BIDDER signup → login
```

and:

```
OFFICER signup → login
```

If the existing login flow does not yet expose the application role, do NOT implement full role retrieval/authorization here.

Instead document the exact requirement for Milestone 04.

==================================================

## 11. DATA ISOLATION

==================================================

Verify that signup creates the correct profile.

Example:

```
BIDDER signup
    ↓
Application User
    role = BIDDER
    ↓
bidder profile
```

It must NOT create:

```
officer profile
```

Likewise:

```
OFFICER signup
    ↓
Application User
    role = OFFICER
    ↓
officer profile
```

It must NOT create:

```
bidder profile
```

Do not allow the frontend to create both profiles.

==================================================

## 12. SECURITY BOUNDARY

==================================================

This milestone establishes account creation only.

Do NOT claim that RBAC is complete.

At the end of this milestone:

```
Authentication
    = existing auth system + new role/profile creation

Authorization
    = NOT YET IMPLEMENTED
```

Milestone 04 will establish:

```
require_role(BIDDER)
require_role(OFFICER)
```

and protected backend endpoints.

Do not prematurely modify every existing API.

==================================================

## 13. TESTS

==================================================

Add focused tests for Milestone 03.

At minimum:

### Signup

Test:

```
valid BIDDER signup → success

valid OFFICER signup → success
```

### Role validation

```
missing role → rejected

invalid role → rejected

ADMIN → rejected

arbitrary role → rejected
```

### Profile creation

```
BIDDER → bidder profile created

BIDDER → officer profile NOT created

OFFICER → officer profile created

OFFICER → bidder profile NOT created
```

### Validation

```
missing required bidder field → rejected

missing required officer field → rejected
```

### Duplicate account

```
existing email → appropriate error
```

### Login compatibility

```
newly created bidder can log in

newly created officer can log in
```

Use the project's existing test fixtures and authentication test patterns.

Do not create a second test infrastructure.

==================================================

## 14. FRONTEND TESTING

==================================================

Verify:

1. Signup page renders.
2. Role selection is visible.
3. Bidder selection displays bidder fields.
4. Officer selection displays officer fields.
5. Switching roles does not retain inappropriate role-specific fields.
6. Submit without selecting a role → validation error.
7. Successful bidder signup → success.
8. Successful officer signup → success.
9. Backend validation errors display correctly.
10. No officer-only information is shown on bidder signup.
11. No bidder-only information is shown on officer signup.

If the project has no frontend test framework, perform a careful manual/browser verification rather than installing a new testing framework solely for this milestone.

==================================================

## 15. DATABASE VERIFICATION

==================================================

After signup tests, verify the database records.

For a bidder:

```
Auth identity
      ↓
Application user
      role = BIDDER
      ↓
Bidder profile
```

For an officer:

```
Auth identity
      ↓
Application user
      role = OFFICER
      ↓
Officer profile
```

Verify that the relationships point to the correct user.

Do not modify existing tender/bid/compliance records.

==================================================

## 16. EXISTING SYSTEM PROTECTION

==================================================

The following systems are PROTECTED:

* Tender management
* Bidder domain logic
* Tender documents
* Bidder documents
* OCR
* AI extraction
* Government verification
* Evidence
* Compliance Engine
* Evidence Resolver
* Evidence traceability
* Officer Review
* Audit
* Task 17 dashboard
* Task 19 processing
* Task 20 workflow

Do not rewrite them.

Do not duplicate them.

Do not move them.

Do not refactor unrelated code.

If a protected module must be changed, explain why before making broad changes.

==================================================

## 17. STRICTLY OUT OF SCOPE

==================================================

DO NOT implement:

* backend RBAC middleware
* `require_role`
* role-protected API groups
* frontend route guards
* bidder dashboard
* officer dashboard
* bidder score page
* bidder failure page
* bid submission workflow
* new landing page
* 3D components
* new government integrations
* compliance changes
* officer decision changes
* Task 04
* Task 05
* Task 06
* Task 07+
* Task 21

ONLY implement Milestone 03 signup functionality.

==================================================

## 18. DOCUMENTATION / HANDOFF

==================================================

Update:

```
docs/RBAC_ARCHITECTURE.md
docs/MILESTONE_STATUS.md
docs/HANDOFF.md
```

Add a Milestone 03 section documenting:

* signup architecture
* role selection
* bidder signup flow
* officer signup flow
* backend validation
* profile creation
* authentication provider
* files changed
* tests
* known limitations
* post-signup behavior
* what Milestone 04 must implement

Update:

```
Current Milestone: 03
Status: COMPLETED
```

Next:

```
Milestone 04 — Authentication + Role Enforcement
```

In `HANDOFF.md`, explicitly state:

```
RBAC authorization is NOT complete.
```

This is important so the next agent does not assume frontend role selection is security.

==================================================

## 19. FINAL REGRESSION

==================================================

Run the focused Milestone 03 tests.

Then run the complete backend regression:

```
pytest -q
```

If frontend code changed, run:

```
npm run build
```

Also run:

```
git diff --check
git status --short
git diff --stat
git diff --name-only
```

Verify:

* no secrets added
* no `.env` changes
* no historical migration modified
* no unrelated files modified
* no protected system rewritten
* no duplicate authentication system
* no duplicate bidder domain system
* no extra roles
* no dashboard work started

==================================================

## 20. NO COMMIT

==================================================

DO NOT commit.

The lead agent will review the work first.

==================================================

## 21. FINAL REPORT

==================================================

End with exactly:

# Milestone 03 Report

## Status

MILESTONE 03 COMPLETED — READY FOR REVIEW

OR:

MILESTONE 03 BLOCKED — DO NOT START MILESTONE 04

## Authentication Used

...

## Role Selection

...

## BIDDER Signup

...

## OFFICER Signup

...

## Database Records

...

## Backend Validation

...

## Tests

...

## Full Regression

...

## Frontend Build

...

## Files Created

...

## Files Modified

...

## Protected Systems Verified

...

## Known Limitations

...

## Important Security Note

State explicitly:

RBAC AUTHORIZATION IS NOT YET IMPLEMENTED.
Milestone 04 is responsible for backend role enforcement.

## Handoff

Next milestone:

Milestone 04 — Authentication + Role Enforcement

## Commit

NOT COMMITTED

STOP HERE.

Do not start Milestone 04.

### The progression is now clean

You want your teammates to work like this:

```text
M1
Architecture
   ↓
M2
Database
   ↓
M3
Signup + role/profile creation
   ↓
M4
Actual backend authorization 🔐
   ↓
M5
Frontend route protection
   ↓
M6
Landing page + 3D
   ↓
M7–9
Bidder product
   ↓
M10
Officer product
   ↓
M11
Security/regression
   ↓
M12
Final integration
```

**The critical distinction is M3 vs M4:** selecting "Officer" on a signup form does **not** constitute RBAC. M3 creates the identity and role correctly; **M4 makes the role actually enforce permissions.**
