Yes! Assuming **Milestone 1 is correctly completed and approved**, Milestone 2 should be **database foundation only**.

The key is: **do not let the agent start signup, authentication, RBAC middleware, or dashboards yet.** Milestone 2 should establish the data model that everything else will safely build on.

Here is the copy-paste prompt for your teammate:

You are continuing the existing SecondLook project.

# MILESTONE 02 — DATABASE USER / ROLE FOUNDATION

Milestone 01 has already been completed and approved.

Before doing anything, read:

* `docs/RBAC_ARCHITECTURE.md`
* `docs/MILESTONE_STATUS.md`
* `docs/HANDOFF.md`

Treat the architecture decisions from Milestone 01 as the source of truth.

Your job in this milestone is ONLY to implement the database foundation required for:

* Bidder accounts
* Officer accounts
* Application roles
* Separate bidder profile data
* Separate officer profile data
* Future role-based authentication

DO NOT implement signup, login, RBAC middleware, frontend role routing, dashboards, landing page, or bid submission in this milestone.

==================================================

## 1. INSPECT BEFORE MODIFYING

==================================================

First inspect the actual current implementation.

Determine:

* How Supabase Auth is currently configured
* Whether `auth.users` is already used
* Whether an application-level users table exists
* Existing bidder table/model
* Existing officer/user/procurement tables
* Existing relationships between users, bidders, tenders and documents
* Existing database migrations
* Existing SQLAlchemy models
* Existing Supabase/RLS configuration
* Existing test database fixtures

DO NOT assume the architecture from Milestone 01 is perfectly implemented.

If the existing project differs from the Milestone 01 design, preserve working architecture and make the minimum safe change.

==================================================

## 2. IMPLEMENT THE ROLE FOUNDATION

==================================================

Implement exactly these application roles:

```
BIDDER
OFFICER
```

Do not introduce:

* ADMIN
* SUPER_ADMIN
* REVIEWER
* AUDITOR
* MANAGER
* any other new role

unless an existing production/data model already requires it.

Use the project's existing enum/type conventions if available.

The role must have a single authoritative representation.

Do NOT allow multiple conflicting role fields to exist.

For example, avoid:

```
users.role
profiles.role
bidder.role
```

all independently controlling authorization.

There should be one authoritative application role.

==================================================

## 3. USER / IDENTITY ARCHITECTURE

==================================================

If Supabase Auth is already the identity provider, do NOT create a second password/authentication system.

Use the existing authentication identity and introduce the minimum application-level data needed for roles/profiles.

Preferred conceptual architecture:

```
Auth Identity
      |
      v
Application User
      |
   role
  /    \
 /      \
```

BIDDER    OFFICER
|          |
v          v
bidder_    officer_
profile    profile

However:

DO NOT blindly create these exact tables.

Use the architecture discovered in Milestone 01 and the actual existing schema.

The important invariants are:

1. One authenticated identity maps to one application user.
2. One application user has exactly one application role.
3. A BIDDER has a bidder profile.
4. An OFFICER has an officer profile.
5. Bidder-specific fields do not belong in officer profiles.
6. Officer-specific fields do not belong in bidder profiles.

==================================================

## 4. BIDDER PROFILE

==================================================

Create or adapt the bidder profile structure according to the existing architecture.

The profile should be capable of supporting future bidder functionality such as:

* company name
* GST number
* PAN number
* contact information
* business information

IMPORTANT:

Do not duplicate data that already exists in the current bidder model.

If an existing `Bidder` entity already contains company information, determine whether:

```
application user
      ↓
bidder entity
```

is safer than creating another duplicate company table.

Avoid creating:

```
bidder_profiles
bidders
bidder_companies
```

all containing the same information.

Reuse existing domain entities where appropriate.

Document the final relationship.

==================================================

## 5. OFFICER PROFILE

==================================================

Create or adapt the officer profile structure according to the existing architecture.

It should be capable of supporting future officer information such as:

* name
* department
* designation
* contact information

Again:

DO NOT duplicate an existing officer/procurement-user entity if one already exists.

Reuse existing models where appropriate.

==================================================

## 6. DATABASE CONSTRAINTS

==================================================

Implement appropriate database-level constraints.

At minimum, ensure:

### User

```
user identity is unique
```

### Role

```
role must be BIDDER or OFFICER
```

### Bidder profile

```
one application user → at most one bidder profile
```

### Officer profile

```
one application user → at most one officer profile
```

### Role/profile consistency

The schema should make it difficult or impossible to create invalid states such as:

```
BIDDER user
   ↓
officer profile only
```

or:

```
OFFICER user
   ↓
bidder profile only
```

If a complete database-level enforcement of role/profile consistency would be unsafe with the current architecture, document the limitation and enforce it at the service layer rather than introducing fragile database triggers.

Do not over-engineer this.

==================================================

## 7. MIGRATION

==================================================

Create the required additive migration(s).

Migration requirements:

* additive where possible
* safe for existing data
* reversible where the project's migration conventions support it
* no destructive changes
* no dropping existing tables
* no renaming existing production columns unless absolutely necessary
* no modification of historical migration files
* no deletion of existing bidder/tender/document data

If existing data must be migrated into the new structure, create a deterministic migration strategy.

DO NOT invent existing users.

DO NOT invent officer accounts.

DO NOT invent bidder accounts.

If backfilling existing records requires a decision that cannot be safely inferred, document it instead of guessing.

==================================================

## 8. SQLALCHEMY MODELS

==================================================

Update/create only the necessary SQLAlchemy models.

Follow the project's existing conventions for:

* UUIDs
* timestamps
* enums
* relationships
* foreign keys
* naming
* Base classes
* indexes

Avoid introducing a new ORM pattern.

Ensure relationships are explicit and understandable.

Example conceptual relationship:

```
ApplicationUser
    |
    +-- bidder_profile
    |
    +-- officer_profile
```

Do not create circular or duplicated ownership relationships.

==================================================

## 9. SUPABASE / RLS

==================================================

Inspect existing Supabase RLS policies.

Do NOT disable RLS.

Do NOT make private bidder information publicly readable.

Design/apply only the minimum policy changes needed for the new user/profile tables.

The future security model must support:

```
Bidder A
   ↓
own profile/data

Bidder A
   X
Bidder B's profile/data

Officer
   ↓
authorized procurement data
```

Do not implement the complete RBAC authorization layer yet.

That belongs to Milestone 04.

If RLS depends on authentication claims that do not yet exist, document that dependency instead of inventing an unsafe workaround.

==================================================

## 10. EXISTING DOMAIN RELATIONSHIPS

==================================================

Inspect how the existing:

* bidders
* tenders
* bidder documents
* compliance evaluations
* evidence
* officer reviews
* audit events

are related.

Do NOT rewrite these systems.

The objective is to establish identity/profile relationships around the existing domain model.

The existing compliance architecture remains protected.

DO NOT MODIFY:

```
backend/app/ocr/
backend/app/ai/
backend/app/government/
backend/app/compliance/
evidence resolver
officer review engine
audit engine
existing workers
existing Task 17 dashboard aggregation
existing Task 19 processing pipeline
existing Task 20 workflow integration
```

unless an absolutely unavoidable import/type change is required.

If such a change is required, stop and document it before making broad changes.

==================================================

## 11. TESTS

==================================================

Add focused tests for the database foundation.

Test at minimum:

### Role creation

```
BIDDER → valid
OFFICER → valid
invalid role → rejected
```

### User/profile relationship

```
BIDDER → bidder profile
OFFICER → officer profile
```

### Uniqueness

```
one user → one bidder profile
one user → one officer profile
```

### Invalid combinations

```
bidder user + officer profile
officer user + bidder profile
```

The tests should reflect the actual architecture.

Do not create fake authentication tests yet.

Authentication belongs to Milestone 03/04.

==================================================

## 12. REGRESSION TEST

==================================================

Run the existing backend test suite.

The existing Tasks 01–20 functionality MUST remain intact.

At minimum run:

```
pytest -q
```

Also run any focused tests created for this milestone.

If frontend code was not changed, do not make unnecessary frontend changes.

If practical, run the frontend build only as a regression confirmation.

==================================================

## 13. DOCUMENTATION / HANDOFF

==================================================

Update:

```
docs/RBAC_ARCHITECTURE.md
docs/MILESTONE_STATUS.md
docs/HANDOFF.md
```

Do NOT remove previous architecture decisions.

Add a section:

```
## Milestone 02 — Database Foundation
```

Document:

* final identity model
* final role model
* bidder profile model
* officer profile model
* relationships
* constraints
* migrations created
* RLS changes
* existing data handling
* tests
* known limitations
* decisions made
* files created
* files modified

For `MILESTONE_STATUS.md`, set:

```
Current Milestone: 02
Status: COMPLETED
```

and:

```
Next Milestone:
Milestone 03 — Signup Role Selection
```

For `HANDOFF.md`, clearly tell the next agent:

* what is now implemented
* what is intentionally NOT implemented
* which files contain the role model
* which migration must be applied
* what Milestone 03 should do
* any unresolved concerns

==================================================

## 14. STRICT FILE / ARCHITECTURE RULE

==================================================

Before changing any file, determine whether it is necessary.

DO NOT perform broad refactoring.

DO NOT reorganize the backend.

DO NOT reorganize the frontend.

DO NOT rename unrelated models.

DO NOT rewrite existing services.

DO NOT duplicate existing bidder entities.

DO NOT duplicate existing authentication.

DO NOT create a second user system.

DO NOT implement authorization yet.

==================================================

## 15. STRICTLY OUT OF SCOPE

==================================================

Do NOT implement:

* signup UI
* login UI
* password handling
* authentication flow
* JWT implementation
* session management redesign
* role middleware
* FastAPI `require_role`
* frontend route guards
* bidder dashboard
* officer dashboard
* landing page
* 3D components
* bid upload UI
* bidder compliance UI
* bidder scoring UI
* officer dashboard UI
* Task 21
* unrelated refactoring

These belong to later milestones.

==================================================

## 16. FINAL VERIFICATION

==================================================

Before declaring completion:

Run:

```
git diff --check
git status --short
git diff --stat
git diff --name-only
```

Verify:

* migration files are correct
* no historical migration was modified
* no secrets were added
* `.env` was not committed
* no generated junk exists
* protected systems remain unchanged
* no duplicate authentication system exists
* no duplicate bidder domain model was accidentally created
* no frontend product work was started

Then run:

```
pytest -q
```

and report the exact result.

==================================================

## 17. DO NOT COMMIT

==================================================

Do NOT create a git commit.

The human/lead agent will review the implementation before committing.

==================================================

## 18. FINAL REPORT FORMAT

==================================================

End with exactly this structure:

# Milestone 02 Report

## Status

MILESTONE 02 COMPLETED — READY FOR REVIEW

OR:

MILESTONE 02 BLOCKED — DO NOT START MILESTONE 03

## Architecture Implemented

...

## Database Changes

...

## Migration Files

...

## Models Changed

...

## RLS Changes

...

## Tests

...

## Full Regression

...

## Files Created

...

## Files Modified

...

## Protected Systems Verified

...

## Known Limitations

...

## Handoff

Next milestone:

Milestone 03 — Signup Role Selection

## Commit

NOT COMMITTED

STOP HERE.

Do not start Milestone 03.

### Why I'm making Milestone 2 this strict

The **most dangerous mistake** here would be your teammate seeing `Bidder`, `Officer`, and `Auth` and immediately creating three new systems.

Your existing project already has a **Bidder domain model**, tender relationships, documents, compliance evaluations, reviews, etc. We want:

**identity → role → existing domain objects**

—not a second parallel procurement system.

Once Milestone 2 is done, send me the agent's report before giving them Milestone 3. **Milestone 3 is where we'll actually touch the signup page.**
