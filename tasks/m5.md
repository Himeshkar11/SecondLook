Absolutely — **Milestone 5 should now be frontend-only role routing and navigation**. 🔐➡️🖥️

Milestone 4 established the real security boundary in the backend. Milestone 5 makes the frontend behave correctly for each role, while **never treating frontend checks as security**.

Give your teammate this exact prompt:

You are continuing the existing SecondLook project.

# MILESTONE 05 — FRONTEND ROLE-BASED ROUTING + NAVIGATION

Milestones 01–04 have already been completed and approved.

Before changing anything, read:

* `docs/RBAC_ARCHITECTURE.md`
* `docs/MILESTONE_STATUS.md`
* `docs/HANDOFF.md`

Inspect the actual implementations from Milestones 01–04 before making changes.

Your ONLY objective in this milestone is:

> Make the React frontend understand the authenticated user's BIDDER/OFFICER role and provide role-appropriate routes, navigation, and route protection.

The backend RBAC from Milestone 04 is the actual security boundary.

Frontend route protection is for UX/navigation and must NEVER replace backend authorization.

==================================================

## 1. INSPECT CURRENT FRONTEND AUTH

==================================================

First inspect the existing frontend.

Determine:

* current authentication state management
* login flow
* signup flow
* token/session storage
* Supabase client usage
* existing auth context/provider
* existing routing
* `App.jsx`
* existing layouts
* existing navigation/sidebar
* existing dashboard routes
* loading states
* logout behavior
* current user retrieval
* how application role is currently obtained

DO NOT create a second authentication state system if one already exists.

Reuse the existing authentication architecture.

==================================================

## 2. ROLE RETRIEVAL

==================================================

The frontend needs to know:

```
authenticated user
      ↓
   application role
      ↓
BIDDER / OFFICER
```

Use the backend/application-user role established in Milestones 02–04.

Do NOT determine the role from:

* signup form state
* URL parameters
* localStorage values supplied by the user
* query parameters
* arbitrary request headers
* a hardcoded username/email
* frontend-only role values

The role must ultimately originate from the authenticated application identity.

If the existing auth/session system does not yet expose the application role, implement the smallest safe mechanism required to retrieve it from the backend.

Do NOT redesign authentication.

==================================================

## 3. CENTRALIZE AUTH STATE

==================================================

If an existing auth provider/context exists:

```
extend it
```

Do not create:

```
AuthContext
UserContext
RoleContext
SessionContext
```

all separately if the existing architecture can handle this centrally.

The frontend should have one clear source for:

```
user
session/authenticated state
role
loading state
```

Conceptually:

```
AuthProvider
   |
   ├── user
   ├── session
   ├── role
   ├── loading
   └── logout()
```

Use the project's actual conventions rather than blindly implementing this exact API.

==================================================

## 4. ROLE VALUES

==================================================

Only support:

```
BIDDER
OFFICER
```

Do not add:

```
ADMIN
SUPER_ADMIN
REVIEWER
AUDITOR
MANAGER
```

etc.

If the backend returns an unknown role:

```
fail safely
```

Do NOT default to OFFICER.

Do NOT default to BIDDER.

Do NOT render privileged pages.

A missing/invalid role should result in a safe state such as:

```
authenticated but role unresolved
```

until the application can resolve it.

==================================================

## 5. ROUTE STRUCTURE

==================================================

Introduce role-aware frontend routing around the existing React router.

Recommended conceptual structure:

PUBLIC:

```
/
/login
/signup
```

BIDDER:

```
/bidder
/bidder/tenders
/bidder/bids
/bidder/compliance
/bidder/documents
/bidder/profile
```

OFFICER:

```
/officer
/officer/tenders
/officer/bidders
/officer/evaluations
/officer/reviews
/officer/audit
```

IMPORTANT:

Do NOT create placeholder pages for every future feature just to make routes exist.

Only create routes/pages necessary for this milestone and compatible with the existing application.

If an existing page already represents a required destination, reuse it.

==================================================

## 6. CREATE CENTRALIZED ROUTE GUARD

==================================================

Implement a centralized frontend route guard using the project's existing routing architecture.

Conceptually:

```
ProtectedRoute
    ↓
Is authenticated?
    ↓
   YES
    ↓
Does role match?
    ↓
   YES
    ↓
  render
```

Otherwise:

```
unauthenticated → /login

wrong role → appropriate role dashboard
```

Do NOT duplicate role checks inside every page.

Avoid code such as:

```
if (role === "BIDDER") ...
```

repeated throughout dozens of components.

Use one reusable routing mechanism.

==================================================

## 7. WRONG-ROLE BEHAVIOR

==================================================

Test and implement:

### Bidder accessing officer route

```
/officer/dashboard
```

must NOT render the officer dashboard.

Redirect to the bidder's appropriate authenticated destination.

### Officer accessing bidder route

```
/bidder/dashboard
```

must NOT render the bidder dashboard.

Redirect to the officer's appropriate authenticated destination.

### Unauthenticated user

```
/bidder/*
/officer/*
```

must redirect to:

```
/login
```

### Unknown role

Do not render either privileged dashboard.

Use a safe fallback/error state consistent with the application.

==================================================

## 8. NAVIGATION

==================================================

Create role-aware navigation.

A bidder should eventually see navigation such as:

```
Dashboard
Tenders
My Bids
Compliance
Documents
Profile
```

An officer should eventually see:

```
Dashboard
Tenders
Bidders
Evaluations
Requirement Review
Audit Trail
Profile
```

IMPORTANT:

This milestone does NOT require fully implementing all these pages.

If a destination is not implemented yet, do not create large placeholder systems.

Navigation should be prepared in a clean, extensible way for Milestones 07–10.

Do not expose officer navigation to bidders.

Do not expose bidder-specific navigation to officers unless intentionally shared.

==================================================

## 9. EXISTING ROUTES

==================================================

Carefully map existing routes from Tasks 01–20.

Do not delete working routes.

Determine which existing pages should become:

```
officer-only
bidder-only
shared
public
```

Use the architecture from Milestone 01.

Where an existing route currently serves officer functionality, preserve its functionality while placing it behind the appropriate frontend route boundary.

Do NOT rewrite the underlying business logic.

==================================================

## 10. EXISTING TASK 17–20 SYSTEMS

==================================================

Preserve the existing:

* Tender Evaluation Dashboard
* Tender Requirement Workspace
* Tender Document Processing
* Tender Workflow
* Compliance
* Evidence
* Officer Review
* Audit Trail

The existing officer-oriented functionality should remain usable.

Where appropriate, classify these existing pages as OFFICER routes.

Do NOT duplicate them.

For example, do NOT create a second:

```
TenderEvaluationDashboard
```

just because it is now under an officer route.

Reuse the existing component.

==================================================

## 11. BIDDER SIDE

==================================================

At this milestone, only establish the routing foundation for bidder functionality.

Do NOT implement the complete bidder dashboard yet.

A bidder should have a valid authenticated destination.

If no bidder dashboard exists, create the smallest appropriate shell/page needed to verify routing.

Example:

```
/bidder
```

can temporarily show:

```
Bidder Workspace

Your bidder account is active.
```

Do NOT implement:

* score calculation
* bid upload
* compliance display
* failure explanation

Those belong to Milestones 07–09.

==================================================

## 12. OFFICER SIDE

==================================================

Likewise, preserve the existing officer functionality.

If `/officer` does not yet exist, create only a minimal officer shell or route that can later become the officer dashboard.

Do NOT build the complete officer dashboard.

That belongs to Milestone 10.

If an existing Task 17 dashboard is the correct current officer destination, reuse it.

==================================================

## 13. AUTHENTICATION LOADING STATE

==================================================

Handle the initial authentication-loading state correctly.

Avoid:

```
page loads
  ↓
role temporarily undefined
  ↓
redirect to login
  ↓
role loads
  ↓
redirect back
```

Do not create redirect flicker.

Use a proper:

```
loading/auth resolving
```

state before making route decisions.

The user should not see a false unauthorized state while authentication is still initializing.

==================================================

## 14. LOGOUT

==================================================

Preserve the existing logout mechanism.

After logout:

```
clear authenticated state
   ↓
clear role/user state
   ↓
redirect to public/login page
```

Verify that pressing browser Back does not expose protected UI as an authenticated page.

Remember:

frontend hiding is not security; backend APIs must still enforce authorization.

==================================================

## 15. DIRECT URL ACCESS

==================================================

Manually test direct navigation.

Examples:

```
http://localhost:5173/officer/...

http://localhost:5173/bidder/...
```

A user must not gain access simply by entering a URL manually.

Test:

```
unauthenticated → bidder URL
unauthenticated → officer URL
bidder → officer URL
officer → bidder URL
```

==================================================

## 16. API ERROR COMPATIBILITY

==================================================

If the backend returns:

```
401
```

the frontend should treat the user as unauthenticated and use the existing login/session recovery behavior.

If the backend returns:

```
403
```

the frontend should show an appropriate unauthorized state or redirect.

Do NOT implement a frontend workaround that attempts to bypass the backend.

Do NOT retry privileged requests with modified roles.

==================================================

## 17. SECURITY RULE

==================================================

NEVER trust frontend role state as authorization.

This is allowed:

```
role === "BIDDER"
   ↓
show bidder navigation
```

This is NOT sufficient:

```
role === "BIDDER"
   ↓
allow sensitive API operation
```

All sensitive API operations remain protected by Milestone 04 backend authorization.

==================================================

## 18. TESTS

==================================================

Add focused frontend tests if the existing project already has a frontend testing framework.

Test:

### Authentication

```
unauthenticated → protected route redirects
```

### Bidder

```
bidder → bidder route → allowed

bidder → officer route → redirected/denied
```

### Officer

```
officer → officer route → allowed

officer → bidder route → redirected/denied
```

### Invalid role

```
unknown role → privileged route not rendered
```

### Loading

```
auth loading → no premature redirect
```

### Logout

```
logout → public route
```

If no frontend test framework exists, do NOT introduce a large new testing infrastructure solely for this milestone.

Perform browser/manual verification instead.

==================================================

## 19. BACKEND REGRESSION

==================================================

Do not modify backend business logic unless absolutely necessary for retrieving the authenticated application role.

Run:

```
pytest -q
```

The entire existing Tasks 01–20 backend suite must continue passing.

If backend changes were made, clearly explain why.

==================================================

## 20. FRONTEND BUILD

==================================================

Run:

```
npm run build
```

The build must pass.

Also run:

```
git diff --check
```

==================================================

## 21. PROTECTED ARCHITECTURE

==================================================

The following systems are PROTECTED:

* OCR
* AI extraction
* Government verification
* Evidence Resolver
* Compliance Engine
* Evidence traceability
* Officer Review
* Audit Service
* Existing workers
* Task 17 dashboard
* Task 19 document processing
* Task 20 workflow

Do not rewrite these systems.

Do not create duplicate implementations.

Do not move them unnecessarily.

==================================================

## 22. STRICTLY OUT OF SCOPE

==================================================

DO NOT implement:

* new signup flow
* new authentication provider
* backend RBAC redesign
* bidder dashboard implementation
* officer dashboard implementation
* bidder scoring
* bidder failure explanations
* bid submission
* bidder document workflow
* new landing page
* 3D components
* new compliance engine
* new government APIs
* new OCR
* new AI provider
* new worker
* admin role
* granular permissions
* organization-level RBAC
* Task 06+
* Task 21

This milestone is ONLY:

```
frontend authentication state
+
role retrieval
+
frontend route protection
+
role-aware navigation
```

==================================================

## 23. DOCUMENTATION

==================================================

Update:

```
docs/RBAC_ARCHITECTURE.md
docs/MILESTONE_STATUS.md
docs/HANDOFF.md
```

Document:

* frontend authentication state
* role retrieval mechanism
* route guard
* public routes
* bidder routes
* officer routes
* shared routes
* navigation behavior
* wrong-role behavior
* logout behavior
* loading behavior
* tests
* files changed
* known limitations

IMPORTANT:

Clearly state:

```
Frontend route protection is implemented for UX/navigation.
Backend RBAC remains the authoritative security boundary.
```

Update:

```
Current Milestone: 05
Status: COMPLETED
```

Next:

```
Milestone 06 — Landing Page + 3D Experience
```

The handoff MUST explicitly tell the next agent that Milestone 06 is responsible for the landing page and 3D experience.

==================================================

## 24. FINAL DIFF AUDIT

==================================================

Run:

```
git status --short
git diff --check
git diff --stat
git diff --name-only
```

Check for:

* accidental `.env` changes
* secrets
* generated files
* unrelated refactoring
* duplicate auth providers
* duplicate role state systems
* duplicate route guards
* changes to protected business logic
* unnecessary backend changes

==================================================

## 25. NO COMMIT

==================================================

DO NOT create a git commit.

The lead agent will review the implementation first.

==================================================

## 26. FINAL REPORT

==================================================

End with exactly:

# Milestone 05 Report

## Status

MILESTONE 05 COMPLETED — READY FOR REVIEW

OR:

MILESTONE 05 BLOCKED — DO NOT START MILESTONE 06

## Authentication State

...

## Role Retrieval

...

## Route Guard

...

## Public Routes

...

## BIDDER Routes

...

## OFFICER Routes

...

## Navigation

...

## Wrong-Role Behavior

...

## Logout Behavior

...

## Loading Behavior

...

## Tests

...

## Backend Regression

...

## Frontend Build

...

## Files Created

...

## Files Modified

...

## Protected Systems Verified

...

## Known Limitations

...

## Important Security Boundary

State explicitly:

FRONTEND ROUTE PROTECTION IS IMPLEMENTED.
BACKEND RBAC IS THE AUTHORITATIVE SECURITY BOUNDARY.

## Handoff

Next milestone:

Milestone 06 — Landing Page + 3D Experience

## Commit

NOT COMMITTED

STOP HERE.

Do not start Milestone 06.

### The key checkpoint after M5

You should be able to test this:

```text
                  LOGIN
                    │
             ┌──────┴──────┐
             │             │
          BIDDER         OFFICER
             │             │
             ▼             ▼
       /bidder          /officer
             │             │
       bidder nav      officer nav
```

And:

```text
BIDDER
  │
  ├── /bidder        ✅
  ├── /bidder/...    ✅
  └── /officer/...   ❌
```

```text
OFFICER
  │
  ├── /officer       ✅
  ├── /officer/...   ✅
  └── /bidder/...    ❌
```

Most importantly, **don't let the teammate start making the dashboards look beautiful yet**. M5 should be boring but rock-solid. Once this is approved, **M6 can focus entirely on the public SecondLook landing page and 3D experience** without simultaneously debugging authentication.
