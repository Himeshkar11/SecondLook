Absolutely! 🔥 **Milestone 7 is the first real Bidder product milestone.** The goal is to give a bidder a proper workspace **without touching the existing compliance engine or officer workflow**.

At the end of M7, a bidder should be able to log in and have a useful home for their company/profile, see their relevant tender/bid workspace structure, and navigate it. **Actual bid upload and compliance scoring stay for M8/M9.**

Give your teammate this exact prompt:

You are continuing the existing SecondLook project.

# MILESTONE 07 — BIDDER PROFILE + BIDDER WORKSPACE

Milestones 01–06 have already been completed and approved.

Before changing anything, read:

* `docs/RBAC_ARCHITECTURE.md`
* `docs/MILESTONE_STATUS.md`
* `docs/HANDOFF.md`

Inspect the actual implementation of Milestones 01–06 before making changes.

Your ONLY objective in this milestone is to build the foundation of the authenticated BIDDER experience:

1. Bidder dashboard shell
2. Bidder profile page
3. Bidder-specific navigation
4. Bidder-specific tender workspace entry point
5. Secure bidder data retrieval
6. Proper integration with the existing authentication/RBAC architecture

DO NOT implement the complete bid submission flow.
DO NOT implement bidder compliance scoring.
DO NOT implement bidder failure analysis.
DO NOT implement the officer dashboard.
DO NOT start Milestone 08.

==================================================

## 1. FIRST: INSPECT EXISTING ARCHITECTURE

==================================================

Before changing anything, inspect:

### Backend

* bidder model/domain entity
* bidder profile created in Milestone 02/03
* authenticated-user dependency
* BIDDER role enforcement from Milestone 04
* ownership checks from Milestone 04
* existing bidder APIs
* existing tender APIs
* existing document APIs
* existing bid-related models, if any
* existing schemas/services/repositories
* existing audit conventions

### Frontend

* authentication state
* current role retrieval
* route guard
* bidder routes from Milestone 05
* current bidder shell/page
* navigation
* API services
* layouts
* existing TenderDetailPage
* existing BidderDetailPage

Reuse existing architecture.

DO NOT create parallel bidder/authentication systems.

==================================================

## 2. DEFINE THE BIDDER WORKSPACE

==================================================

The bidder should have a dedicated authenticated workspace.

Conceptually:

```
/bidder

BIDDER DASHBOARD

Welcome, [Company Name]

┌────────────────────┐
│ Active Tenders     │
│        0           │
└────────────────────┘

┌────────────────────┐
│ My Bids            │
│        0           │
└────────────────────┘

┌────────────────────┐
│ Compliance         │
│        —           │
└────────────────────┘

┌────────────────────┐
│ Documents          │
│        0           │
└────────────────────┘

Recent Activity
...
```

This is a workspace foundation.

Do NOT fake real statistics.

If the underlying functionality does not yet exist, show:

```
—
```

or an honest empty state.

Do not fabricate numbers.

==================================================

## 3. BIDDER NAVIGATION

==================================================

Create a clean bidder-specific navigation.

Recommended structure:

```
Dashboard
Tenders
My Bids
Compliance
Documents
Profile
```

IMPORTANT:

Only implement destinations that actually exist.

For future destinations that are not yet implemented, either:

* leave them out until their milestone, or
* provide a clearly labelled disabled/coming-soon state.

Do NOT create fake functionality.

The bidder must NOT see officer navigation such as:

```
Requirement Review
Officer Review
Audit Administration
Officer Dashboard
```

unless the existing architecture explicitly defines them as shared.

==================================================

## 4. BIDDER DASHBOARD

==================================================

Build the initial bidder dashboard.

It should include:

### Header

```
Welcome, [Company Name]
```

### Account summary

```
Company
GST
PAN
Account Type: Bidder
```

Do not display sensitive information unnecessarily.

Mask sensitive identifiers where appropriate.

For example:

```
GST: 29******Z5
```

rather than exposing unnecessary full identifiers if the UI does not require them.

### Workspace cards

Show useful high-level sections for:

```
Active Tenders
My Bids
Compliance
Documents
```

Use real API data where the required backend functionality already exists.

Do not create duplicate business logic simply to calculate dashboard values.

==================================================

## 5. BIDDER PROFILE

==================================================

Create a bidder profile page.

Recommended route:

```
/bidder/profile
```

Display the actual bidder profile fields established in Milestone 02/03.

Potential fields:

```
Company Name
GST Number
PAN Number
Contact Information
Business Information
```

Use the actual schema.

Do NOT invent fields.

Do NOT duplicate the bidder profile into a second table.

==================================================

## 6. PROFILE EDITING

==================================================

Determine whether editing the bidder profile is appropriate based on the existing architecture.

If profile editing is safe and supported:

```
GET profile
PATCH/PUT profile
```

If editing is not yet appropriate because important fields are verified/government-controlled, make those fields read-only and document why.

IMPORTANT:

Do not allow a bidder to modify government verification results.

Do not allow a bidder to modify compliance evaluation results.

Do not allow a bidder to modify evidence snapshots.

Do not allow a bidder to modify officer decisions.

Only profile-owned information may be editable.

==================================================

## 7. BACKEND BIDDER PROFILE API

==================================================

If an appropriate bidder profile API already exists:

```
reuse it.
```

If it does not exist, create the smallest necessary API.

Prefer:

```
GET /api/v1/bidders/me
```

or an equivalent route consistent with the existing architecture.

For editing, if justified:

```
PATCH /api/v1/bidders/me
```

Do NOT expose an arbitrary:

```
/bidders/{bidder_id}
```

endpoint to the frontend for self-service unless ownership is enforced.

The backend must determine the bidder from the authenticated user.

Do NOT trust:

```
bidder_id
```

from the frontend as proof of ownership.

==================================================

## 8. OWNERSHIP SECURITY

==================================================

This is mandatory.

A bidder can only access their own profile/workspace data.

Test:

```
Bidder A → own profile → ALLOWED

Bidder A → Bidder B profile → DENIED
```

Do not rely on frontend route hiding.

Backend must enforce ownership.

If a bidder attempts:

```
GET /api/v1/bidders/B
```

while authenticated as bidder A,

the backend must not expose B's private information.

==================================================

## 9. BIDDER → OFFICER PROTECTION

==================================================

Verify Milestone 04 still works.

A bidder must not be able to access officer functionality.

Test at least one representative officer endpoint:

```
BIDDER → officer endpoint → 403
```

Do not modify the RBAC architecture unless necessary.

If the test fails, fix the underlying regression before continuing.

==================================================

## 10. TENDER DISCOVERY FOUNDATION

==================================================

The bidder dashboard should have a clear path to:

```
Browse Tenders
```

If the existing tender listing API can safely provide public/eligible tender information to bidders, reuse it.

Do not create a duplicate tender system.

If tender access currently assumes officer access only, do NOT weaken that access model.

Instead:

* document the required bidder tender-read API
* create only the minimum read-only endpoint if the existing architecture clearly supports it

The bidder must NOT gain tender-management capabilities.

Bidder access should be read-only at this stage.

==================================================

## 11. BIDDER TENDER VIEW

==================================================

If the existing TenderDetailPage can safely be reused:

```
reuse it.
```

Adapt only what is necessary for bidder viewing.

A bidder should eventually be able to understand:

```
Tender title
Description
Requirements
Important information
Bid submission status
```

For this milestone, only implement information that already exists in the backend.

DO NOT implement actual bid submission.

DO NOT create bidder-specific copies of tender requirements.

==================================================

## 12. EMPTY STATES

==================================================

A new bidder account will probably have no data.

Design useful empty states.

Example:

```
No active bids yet.

Browse available tenders to find an opportunity.

[ Browse Tenders ]
```

For documents:

```
No bid documents uploaded yet.
```

For compliance:

```
No compliance evaluation available yet.
```

Do NOT show fake:

```
100% score
5 bids
12 tenders
```

etc.

==================================================

## 13. COMPLIANCE PLACEHOLDER

==================================================

The bidder navigation may include:

```
Compliance
```

but do NOT implement the actual bidder compliance experience yet.

If needed, create a minimal page:

```
Compliance

Your compliance results will appear here
after a bid evaluation is completed.
```

This is only a placeholder.

Do NOT calculate a score here.

Do NOT duplicate Task 11 logic.

Do NOT modify the ComplianceEngine.

Milestone 09 will build the actual bidder compliance result experience.

==================================================

## 14. MY BIDS PLACEHOLDER

==================================================

Likewise, `/bidder/bids` may be created as a minimal workspace entry if the existing project supports it.

Do not implement full bid submission.

If no bid model/workflow is currently suitable, display an honest empty state.

Milestone 08 will implement the actual bid submission workflow.

==================================================

## 15. DOCUMENTS PLACEHOLDER

==================================================

Create a bidder-facing Documents entry only if it can safely reuse the existing document system.

Do NOT create another document storage system.

Do NOT duplicate:

* Supabase Storage
* document models
* OCR
* processing workers

For now, it can show:

```
Bid Documents

No documents uploaded yet.
```

The actual upload/processing workflow belongs to Milestone 08.

==================================================

## 16. API DESIGN

==================================================

Prefer a small number of well-defined bidder APIs.

Do NOT create an API for every dashboard card.

For example:

```
GET /api/v1/bidders/me
```

and reuse existing APIs for:

```
tenders
bids
documents
compliance
```

where appropriate.

Avoid frontend N+1 requests.

If dashboard statistics require aggregation, use an appropriate backend endpoint or existing service rather than making the browser request dozens of individual resources.

Do not duplicate Task 17's officer dashboard aggregation.

==================================================

## 17. AUTHENTICATION STATE

==================================================

Reuse Milestone 05 authentication state.

Do not create another:

```
AuthContext
```

Do not create another role store.

Use the existing authenticated user and role.

The bidder dashboard should only render when:

```
authenticated
AND
role == BIDDER
```

Backend authorization remains mandatory.

==================================================

## 18. OFFICER PROTECTION

==================================================

Verify:

```
OFFICER → /bidder
   ↓
redirected/denied
```

and:

```
BIDDER → /officer
   ↓
redirected/denied
```

Do not weaken the route guard.

==================================================

## 19. DESIGN

==================================================

Use the existing SecondLook design language from Milestone 06.

The bidder workspace should feel like the same product as the public landing page.

Visual direction:

```
clean
professional
trustworthy
procurement-focused
evidence-driven
```

Do not introduce another design system.

Do not use excessive animations.

Do not add 3D components to the dashboard.

The 3D experience belongs to the public landing page.

==================================================

## 20. RESPONSIVE DESIGN

==================================================

The bidder workspace must work on:

* desktop
* laptop
* tablet
* mobile

Verify:

* sidebar/navigation
* dashboard cards
* tables/lists
* profile form
* empty states
* tender cards

No horizontal overflow.

==================================================

## 21. ACCESSIBILITY

==================================================

Verify:

* semantic headings
* accessible buttons
* keyboard navigation
* form labels
* focus states
* readable contrast
* meaningful empty-state messages

Do not sacrifice accessibility for visual effects.

==================================================

## 22. TESTS

==================================================

Add focused tests.

### Authentication

```
unauthenticated → /bidder → denied/redirected
```

### Role

```
bidder → /bidder → allowed

officer → /bidder → denied/redirected
```

### Profile

```
bidder A → own profile → success

bidder A → bidder B profile → denied
```

### Profile data

```
bidder profile loads correctly

officer profile does not appear in bidder workspace
```

### API security

```
bidder cannot access officer endpoint
```

### Empty state

```
new bidder with no bids → valid empty dashboard

new bidder with no documents → valid empty state
```

Do not create fake test data merely to make the UI show impressive statistics.

==================================================

## 23. REGRESSION

==================================================

Run:

```
pytest -q
```

Then:

```
npm run build
```

Also:

```
git diff --check
```

The existing Tasks 01–20 tests must continue passing.

Do not fix unrelated pre-existing failures unless necessary to this milestone.

==================================================

## 24. PROTECTED SYSTEMS

==================================================

The following are PROTECTED:

* Authentication architecture
* Backend RBAC
* Role model
* Tender domain
* OCR
* AI extraction
* Government verification
* Evidence Resolver
* Compliance Engine
* Evidence traceability
* Officer Review
* Audit Service
* Existing workers
* Task 17 dashboard
* Task 19 processing
* Task 20 workflow
* Supabase Storage

Do NOT rewrite or duplicate these systems.

==================================================

## 25. STRICTLY OUT OF SCOPE

==================================================

DO NOT implement:

* actual bid upload
* bid submission
* bid document processing workflow
* bidder OCR workflow
* bidder AI extraction
* bidder compliance scoring
* bidder failure analysis
* bidder evidence viewer redesign
* officer dashboard
* officer analytics
* officer review changes
* landing page changes
* 3D changes
* new authentication
* new RBAC
* new role
* new compliance engine
* new government provider
* new OCR
* new AI provider
* new worker
* Task 08
* Task 09
* Task 10
* Task 21

This milestone is ONLY:

```
BIDDER WORKSPACE FOUNDATION
+
BIDDER PROFILE
+
BIDDER NAVIGATION
+
SECURE BIDDER DATA ACCESS
+
TENDER DISCOVERY FOUNDATION
```

==================================================

## 26. DOCUMENTATION / HANDOFF

==================================================

Update:

```
docs/RBAC_ARCHITECTURE.md
docs/MILESTONE_STATUS.md
docs/HANDOFF.md
```

Document:

* bidder workspace architecture
* bidder profile API
* bidder ownership enforcement
* bidder routes
* bidder navigation
* tender access
* placeholder pages
* files created
* files modified
* tests
* known limitations
* what remains for Milestone 08
* what remains for Milestone 09

Set:

```
Current Milestone: 07
Status: COMPLETED
```

Next:

```
Milestone 08 — Bid Submission + Bidder Document Workflow
```

The handoff MUST explicitly state:

```
Bidder workspace foundation is implemented.
Actual bid submission is NOT implemented.
Bidder compliance scoring is NOT implemented.
Officer dashboard is NOT implemented.
```

==================================================

## 27. FINAL DIFF AUDIT

==================================================

Run:

```
git status --short
git diff --check
git diff --stat
git diff --name-only
```

Verify:

* no `.env` changes
* no secrets
* no generated junk
* no unrelated refactoring
* no duplicate auth system
* no duplicate bidder model
* no duplicate document system
* no compliance changes
* no officer workflow changes
* no landing page changes
* no new government integration

==================================================

## 28. NO COMMIT

==================================================

DO NOT create a git commit.

The lead agent will review the milestone first.

==================================================

## 29. FINAL REPORT

==================================================

End with exactly:

# Milestone 07 Report

## Status

MILESTONE 07 COMPLETED — READY FOR REVIEW

OR:

MILESTONE 07 BLOCKED — DO NOT START MILESTONE 08

## Bidder Workspace

...

## Bidder Profile

...

## Bidder Navigation

...

## Tender Discovery

...

## Backend APIs

...

## Ownership Security

...

## Role Protection

...

## Empty States

...

## Tests

...

## Full Backend Regression

...

## Frontend Build

...

## Files Created

...

## Files Modified

...

## Protected Systems Verified

...

## Known Limitations

...

## Important Boundary

State explicitly:

BIDDER WORKSPACE FOUNDATION IS IMPLEMENTED.
ACTUAL BID SUBMISSION IS NOT IMPLEMENTED.
BIDDER COMPLIANCE SCORING IS NOT IMPLEMENTED.
OFFICER DASHBOARD IS NOT IMPLEMENTED.

## Handoff

Next milestone:

Milestone 08 — Bid Submission + Bidder Document Workflow

## Commit

NOT COMMITTED

STOP HERE.

Do not start Milestone 08.

### 🎯 M7 checkpoint

When this is done, you should have:

```text
Login
  ↓
BIDDER
  ↓
/bidder
  │
  ├── Dashboard
  │
  ├── Tenders
  │
  ├── My Bids
  │
  ├── Compliance
  │
  ├── Documents
  │
  └── Profile
```

But **don't expect the bidder to submit a bid yet**.

That's deliberately M8:

```text
M7
Identity + Workspace
       ↓
M8
Bid Submission + Documents
       ↓
M9
Compliance Score + Failure Explanation
```

This separation is important because M8 will touch the existing document/OCR/AI pipeline, while M9 will consume the existing compliance/evidence architecture. Keeping them separate makes debugging and teammate handoffs much easier.
