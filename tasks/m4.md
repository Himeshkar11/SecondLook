Absolutely. 🔐 **Milestone 4 is the most important security milestone so far.** This is where the `BIDDER` / `OFFICER` roles created in Milestone 3 actually become enforced permissions.

The teammate should **not build dashboards or frontend route guards yet**. Keep this one strictly backend authorization so it's easy to test and doesn't disturb Tasks 01–20.

Copy-paste this into Antigravity:

You are continuing the existing SecondLook project.

# MILESTONE 04 — AUTHENTICATION + BACKEND ROLE ENFORCEMENT

Milestones 01–03 have already been completed and approved.

Before doing anything, read:

* `docs/RBAC_ARCHITECTURE.md`
* `docs/MILESTONE_STATUS.md`
* `docs/HANDOFF.md`

Inspect the actual implementation of Milestones 01–03 before modifying anything.

Your ONLY goal in this milestone is:

> Make the backend securely identify the authenticated user and enforce the application's BIDDER / OFFICER roles on protected API endpoints.

This is a SECURITY milestone.

Do not build dashboards.
Do not redesign signup.
Do not redesign login.
Do not implement frontend route guards.
Do not implement the landing page.
Do not implement bidder scoring UI.
Do not start Milestone 05.

==================================================

## 1. FIRST: AUDIT CURRENT AUTH

==================================================

Inspect the actual authentication implementation.

Determine:

* Authentication provider
* Supabase Auth configuration
* JWT/session mechanism
* How frontend obtains authentication credentials
* How backend receives credentials
* Existing FastAPI dependencies
* Existing authentication utilities
* Existing user lookup logic
* Existing application-user model
* Existing role field
* Existing bidder/officer profile relationships
* Existing protected endpoints

Do NOT create a second authentication mechanism.

If authentication already exists, extend it.

If there is an existing auth dependency, reuse it rather than creating competing implementations.

Document the current flow before changing it.

==================================================

## 2. ESTABLISH ONE AUTHENTICATED USER DEPENDENCY

==================================================

Create or consolidate a single backend mechanism that answers:

```
Who is making this request?
```

Conceptually:

```
HTTP Request
     ↓
Authentication token
     ↓
Verify token
     ↓
Identify authenticated user
     ↓
Application User
     ↓
role
```

Use the existing project's conventions.

The authenticated-user dependency should expose enough information for downstream authorization without repeatedly decoding tokens throughout individual endpoints.

Do NOT copy authentication logic into every router.

Do NOT manually trust a user ID supplied in the request body.

The authenticated identity must come from the verified authentication mechanism.

==================================================

## 3. ROLE ENFORCEMENT

==================================================

Implement centralized backend role enforcement.

Conceptually support:

```
require_role(BIDDER)
```

and:

```
require_role(OFFICER)
```

Use the project's existing FastAPI dependency architecture if one exists.

The exact implementation can differ, but the principle is mandatory:

```
Authentication
    ≠
Authorization
```

Authentication answers:

```
"Who are you?"
```

Authorization answers:

```
"Are you allowed to do this?"
```

==================================================

## 4. ONLY TWO ROLES

==================================================

The only application roles are:

```
BIDDER
OFFICER
```

Reject unknown roles.

Do NOT add:

* ADMIN
* SUPER_ADMIN
* REVIEWER
* AUDITOR
* PROCUREMENT_MANAGER
* MANAGER
* MODERATOR

unless such a role already exists in the project and Milestone 01 explicitly documented it.

Do not silently fall back to another role.

Do not treat an unknown role as OFFICER.

Do not treat a missing role as BIDDER.

A missing/invalid role should fail authorization safely.

==================================================

## 5. PROTECT EXISTING ENDPOINTS CAREFULLY

==================================================

Do NOT blindly put role checks on every existing endpoint.

First classify existing APIs.

Use the architecture from Milestone 01.

Potential classification:

### Officer-only

Tender management operations.

Requirement approval/rejection.

Officer review.

Officer decision.

Tender-level administrative operations.

Audit operations where appropriate.

Government verification operations where appropriate.

### Bidder-only

Future bidder-specific operations.

Bid submission operations if already present.

Bidder-owned document operations.

Bidder-owned profile operations.

### Shared / authenticated

Read-only operations that genuinely need to be accessible by both roles.

### Public

Public landing/authentication endpoints.

The classification must follow the actual existing application architecture.

DO NOT break Task 01–20 endpoints just to demonstrate RBAC.

==================================================

## 6. MOST IMPORTANT: RESOURCE OWNERSHIP

==================================================

Role checking alone is NOT sufficient.

A bidder must not be able to access another bidder's data.

For bidder-owned resources, enforce:

```
authenticated_user
      ↓
bidder_profile
      ↓
bidder/domain entity
      ↓
requested resource
```

Example:

```
Bidder A requests Bidder B's document
```

Result:

```
403 Forbidden
```

or:

```
404 Not Found
```

according to the existing security convention.

Do NOT simply check:

```
bidder_id == request.path.bidder_id
```

because the path parameter itself is untrusted.

Resolve the bidder associated with the authenticated user first.

==================================================

## 7. TENDER OWNERSHIP / AUTHORIZATION

==================================================

Inspect how officers relate to tenders.

Do NOT invent organization/department permissions unless the existing architecture already supports them.

For now, implement the minimum authorization model established by Milestone 01.

If all authenticated officers are currently intended to access procurement data, document that.

If tender ownership/organization checks already exist, reuse them.

Do NOT create a new organization/RBAC hierarchy.

Future granular permissions can be added later.

==================================================

## 8. BIDDER DATA ISOLATION

==================================================

Explicitly test these cases:

```
Bidder A → own profile        ALLOWED
Bidder A → own documents      ALLOWED
Bidder A → own bid            ALLOWED

Bidder A → Bidder B profile   DENIED
Bidder A → Bidder B documents DENIED
Bidder A → Bidder B bid       DENIED
```

A bidder must NEVER be able to bypass ownership by changing:

```
user_id
bidder_id
document_id
evaluation_id
```

in the URL/body/query parameters.

==================================================

## 9. OFFICER ACCESS

==================================================

Officers should be able to access the officer-authorized procurement APIs defined by the architecture.

Test:

```
OFFICER → officer endpoint       ALLOWED
```

and:

```
BIDDER → officer endpoint        DENIED
```

Do not allow a bidder to become an officer by modifying:

```
role=BIDDER
```

to:

```
role=OFFICER
```

in an HTTP request.

The role must come from the trusted application-user record/authentication architecture.

==================================================

## 10. BIDDER → OFFICER ATTACK TESTS

==================================================

Explicitly test common privilege-escalation attempts.

Examples:

### Request body

```
{
    "role": "OFFICER"
}
```

must NOT elevate a bidder.

### URL

```
/officer/...
```

must NOT bypass authorization.

### Query parameter

```
?role=OFFICER
```

must NOT bypass authorization.

### Header

```
X-Role: OFFICER
```

must NOT bypass authorization.

### User ID

Changing:

```
user_id=A
```

to:

```
user_id=OFFICER_USER_ID
```

must NOT bypass authentication/authorization.

### Bidder ID

Changing:

```
bidder_id=A
```

to:

```
bidder_id=B
```

must NOT expose Bidder B.

==================================================

## 11. HTTP STATUS CONVENTIONS

==================================================

Follow the project's existing API error conventions.

Generally:

```
unauthenticated
    → 401

authenticated but wrong role
    → 403

authenticated and correct role but resource not accessible
    → 403 or 404 according to existing security convention
```

Do not leak sensitive information through error messages.

Do not expose:

* SQL errors
* stack traces
* internal IDs unnecessarily
* tokens
* secrets
* authentication internals

==================================================

## 12. FRONTEND

==================================================

DO NOT implement frontend route guards in this milestone.

DO NOT create:

```
ProtectedRoute.jsx
```

unless one already exists and only requires a tiny compatibility change.

Milestone 05 will handle frontend role-based routing.

The frontend may continue behaving as before.

The backend must be the security boundary.

==================================================

## 13. EXISTING COMPLIANCE ARCHITECTURE

==================================================

The following systems are PROTECTED:

* OCR
* AI extraction
* Government verification
* Evidence Resolver
* Compliance Engine
* Evidence traceability
* Officer Review
* Audit Service
* Existing workers
* Task 17 dashboard aggregation
* Task 19 document processing
* Task 20 workflow integration

Do NOT rewrite these systems.

Do NOT create:

* another compliance engine
* another review engine
* another evidence system
* another audit system
* another worker
* another government provider system

If role enforcement requires a router-level dependency, implement it at the boundary instead of modifying the internal business logic.

==================================================

## 14. AUDIT SECURITY-SENSITIVE ACTIONS

==================================================

Inspect the existing AuditService.

Where the existing architecture already audits sensitive actions, preserve it.

Do not create a second audit system.

If role-protected operations already produce audit events, verify that authorization does not bypass those events.

Do NOT add arbitrary audit events everywhere.

Keep this milestone focused on authentication/authorization.

==================================================

## 15. TEST FIXTURES

==================================================

Create/reuse test fixtures for:

```
authenticated bidder
authenticated officer
unauthenticated request
```

Use the project's existing test infrastructure.

Do not create a separate authentication testing framework.

If real Supabase authentication is unsuitable for unit/integration tests, use deterministic test authentication fixtures/mocks at the existing authentication boundary.

Do NOT weaken production security to make tests easier.

==================================================

## 16. REQUIRED SECURITY TEST MATRIX

==================================================

At minimum test:

### Authentication

```
no credentials → 401

invalid credentials/token → 401

valid bidder → authenticated

valid officer → authenticated
```

### Role authorization

```
bidder → bidder endpoint → PASS

bidder → officer endpoint → 403

officer → officer endpoint → PASS

officer → bidder-only endpoint → 403
```

### Ownership

```
bidder A → own resource → PASS

bidder A → bidder B resource → DENIED
```

### Privilege escalation

```
bidder + role=OFFICER → DENIED

bidder + X-Role=OFFICER → DENIED

bidder + another user_id → DENIED

bidder + another bidder_id → DENIED
```

### Invalid role

```
invalid role → DENIED

missing role → DENIED
```

### Existing functionality

Existing authorized flows continue to work.

==================================================

## 17. REGRESSION TESTS

==================================================

Run the focused authentication/authorization tests first.

Then run the entire backend suite:

```
pytest -q
```

The existing Tasks 01–20 functionality MUST continue passing.

If frontend source was not changed, do not make unnecessary frontend modifications.

If frontend source changed for unavoidable compatibility, run:

```
npm run build
```

Also run:

```
git diff --check
```

==================================================

## 18. SECURITY AUDIT

==================================================

After implementation, manually inspect the code for:

* authentication duplicated across routers
* role checks duplicated across endpoints
* user ID trusted from request body
* bidder ID trusted from request body
* role trusted from frontend
* role trusted from request headers
* role trusted from query parameters
* missing ownership checks
* unauthenticated endpoints accidentally exposed
* officer APIs accessible to bidders
* bidder APIs accessible to officers
* secrets in source code
* debug authentication bypasses
* test-only bypasses leaking into production code

Search explicitly for suspicious patterns such as:

```
request.role
body.role
query.role
X-Role
user_id from untrusted input
bidder_id without ownership validation
```

Do not rely only on automated tests.

==================================================

## 19. DATABASE / MIGRATION

==================================================

Do not create a new migration unless Milestone 04 genuinely requires one.

Milestone 02 established the role/profile database foundation.

Prefer using that existing structure.

If a migration is absolutely necessary:

* make it additive
* do not modify historical migrations
* do not destroy existing data
* document why it is required

Do not change the role model unnecessarily.

==================================================

## 20. DOCUMENTATION

==================================================

Update:

```
docs/RBAC_ARCHITECTURE.md
docs/MILESTONE_STATUS.md
docs/HANDOFF.md
```

Document:

### Authentication

* how the backend identifies users
* authentication provider
* token/session verification
* authenticated-user dependency

### Authorization

* role enforcement mechanism
* BIDDER permissions
* OFFICER permissions
* ownership enforcement

### Protected APIs

List the APIs changed/protected.

### Security Tests

List the attack scenarios tested.

### Known Limitations

Especially document any areas where authorization is intentionally deferred.

Update:

```
Current Milestone: 04
Status: COMPLETED
```

Next:

```
Milestone 05 — Frontend Role-Based Routing
```

The handoff MUST explicitly state:

```
Backend RBAC is implemented.
Frontend route protection is NOT implemented yet.
```

==================================================

## 21. STRICTLY OUT OF SCOPE

==================================================

DO NOT implement:

* new signup UI
* new login UI
* bidder dashboard
* officer dashboard
* frontend route guards
* ProtectedRoute component
* landing page redesign
* 3D components
* bidder scoring UI
* bidder failure UI
* bid submission UI
* new compliance logic
* new government integrations
* new OCR
* new AI provider
* new worker
* new audit system
* organization hierarchy
* admin role
* granular permissions
* Task 05
* Task 06
* Task 21

ONLY implement backend authentication/authorization required for Milestone 04.

==================================================

## 22. FINAL FILE AUDIT

==================================================

Run:

```
git status --short
git diff --check
git diff --stat
git diff --name-only
```

Confirm:

* no `.env` changes
* no secrets
* no generated files
* no unrelated refactoring
* no historical migration modifications
* protected Task 01–20 systems remain intact
* no duplicate authentication system
* no duplicate authorization system
* no duplicate bidder domain model

==================================================

## 23. NO COMMIT

==================================================

DO NOT create a git commit.

The lead agent will review the changes first.

==================================================

## 24. FINAL REPORT

==================================================

End with exactly this structure:

# Milestone 04 Report

## Status

MILESTONE 04 COMPLETED — READY FOR REVIEW

OR:

MILESTONE 04 BLOCKED — DO NOT START MILESTONE 05

## Authentication Architecture

...

## Authorization Architecture

...

## BIDDER Permissions

...

## OFFICER Permissions

...

## Ownership Enforcement

...

## Protected APIs

...

## Security Tests

...

## Privilege Escalation Tests

...

## Full Regression

...

## Frontend Build

...

## Files Created

...

## Files Modified

...

## Database/Migration Changes

...

## Protected Systems Verified

...

## Known Limitations

...

## Important Boundary

State explicitly:

BACKEND RBAC IS IMPLEMENTED.
FRONTEND ROLE-BASED ROUTING IS NOT YET IMPLEMENTED.

## Handoff

Next milestone:

Milestone 05 — Frontend Role-Based Routing

## Commit

NOT COMMITTED

STOP HERE.

Do not start Milestone 05.

### 🔐 What Milestone 4 should accomplish

After this milestone, the system should conceptually behave like:

```text
                 REQUEST
                    │
                    ▼
              Authenticate
                    │
              ┌─────┴─────┐
              │           │
           BIDDER       OFFICER
              │           │
              ▼           ▼
       Bidder APIs     Officer APIs
              │           │
              ▼           ▼
        Ownership       Permission
          checks          checks
```

So even if someone manually calls:

```text
POST /officer/something
```

or changes:

```text
role=OFFICER
```

in the browser, **the backend doesn't trust them**.

That's the foundation we need before Milestone 5 starts hiding/showing pages based on roles.
